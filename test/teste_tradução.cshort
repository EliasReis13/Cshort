int x;
int y;

int main(void) {
    x = 5;
    y = 20;

    while (x > 0) {
        y = y + x * 2;
        if (y > 50) {
            x = 0; 
        } else {
            x = x - 1;
        }
    }
    return 0;
}