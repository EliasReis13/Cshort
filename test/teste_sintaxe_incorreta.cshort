int x // ERRO: Falta o ponto e vírgula

float func(int a, ) { // ERRO: vírgula extra
    return a;
}

int main() {
    int y = 10;
    
    if (y > 5 { // ERRO: Falta o parêntese de fechamento
        y = 0
    } // ERRO: Falta o ponto e vírgula dentro do if
    
    while (y < 1); // ERRO: Ponto e vírgula após a condição de um laço com corpo
    {
        y = y + 1;
    }
    
    return "oi"; // ERRO: Tipo de retorno incompatível (se você já tiver análise semântica)
                 // Para o sintático, isso pode estar correto dependendo da gramática
}